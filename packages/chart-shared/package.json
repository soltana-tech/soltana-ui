{
  "name": "@soltana-ui/chart-shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities for Soltana charting packages. Exports raw .ts source (no build step) because this package is internal-only and consumed exclusively by sibling workspace packages that compile it themselves.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./test-helpers": "./src/test-helpers.ts",
    "./test-static-exports": "./src/test-static-exports.ts"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "pnpm run lint:ts",
    "lint:ts": "eslint src/",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
